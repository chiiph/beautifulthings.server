apiVersion: v1
kind: Service
metadata:
  name: {{ template "beautifulthings.fullname" . }}
  labels:
    app: {{ template "beautifulthings.name" . }}
    chart: {{ template "beautifulthings.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "beautifulthings.name" . }}
    release: {{ .Release.Name }}
